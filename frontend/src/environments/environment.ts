export const environment = {
  production: true,
  // Phase 6 (deployment): replace with the deployed FastAPI backend URL (Render/Railway/Fly.io).
  apiBaseUrl: 'https://api.vistanova.example.com',
};
